## 完了前チェックリスト

<important if="before completing implementation">

**Check each item before completing. Correct any errors.**

| # | Check (correct until it says Yes) |
|---|--------------------------------|
| 1 | All structures are created using CustomClass/`src/scss` (they are not built solely with Tailwind)|
| 2 | The component name will be the same as the CustomClass name, or the same as the element name if no class definition exists. |
| 3 | You must not use the `text-*` class without permission.|
| 4 | You must not overwrite variables using the `[...]` class without permission.|
| 5 | Colors not defined in the variables cannot be used.|

</important>