既存ポートフォリオ文章（現状）に、山水園テーマ構築を通じて得た学習内容を追記するための下書き。

## 現状のポートフォリオ文章

### WordPress

実務で触れる機会のなかったWordPressの経験を積む為、静的htmlサイトからWordPressテーマ作成を行なっています。

・Local / Xserver / 独自ドメイン で実務を想定する
・WordPress / PHP の作法を学ぶ
・ACF / CPTの使用、技術選定
・AI駆動管理画面編集、デプロイ、学習用ドキュメント作成

CMS利用経験、CMS制作経験、AI駆動開発経験により比較的早く環境に合わせた構築ができると考えます。

---

## 追記案（本テーマ構築で得た学習内容）

・要件変化に応じた設計変更の経験：カスタム投稿タイプ（CPT）で客室・お知らせを設計後、管理のしやすさを優先して「標準投稿+カテゴリー+ACF」方式へ移行。既存データ（画像・メタデータ込み）を無停止で移行するスクリプトを実装し、並び順UIはSimple Custom Post Orderプラグインで解決。CPTと標準投稿のどちらが適切かをプロジェクトの要件に応じて判断できるようになった。

・ACF（Advanced Custom Fields）の実践的な設計・実装：Options Page（サイト共通情報）、タブUI（`type => 'tab'`）によるフィールドグループの整理、Gallery/Repeaterフィールドを用いたクライアント編集可能な管理画面構築。`get_field()` がDBに値がなくても `default_value` を返す仕様に起因する「空欄判定バグ」を実データ比較で特定し、`metadata_exists()` / `get_option()` によるDB生値判定に修正。

・WordPress標準関数特有の実装差分のデバッグ：`wp_nav_menu()` が `<li>` で要素をラップすることに起因するCSSレイアウト崩れなど、静的HTMLをそのまま移植しただけでは再現できない箇所を特定・修正する経験。

・デプロイ運用における安全対策：rsyncベースのデプロイスクリプトで除外設定漏れにより開発用ディレクトリを本番へ誤アップロードした経験から、除外リストの整備とdry-run運用の徹底を学習。ローカル（Local by Flywheel）と本番（Xserver）の環境差異（ポート番号の混同など）によるトラブルシューティングも経験。

・セキュリティ意識：SSHホスト名・ユーザー名・サーバーパス等の機密情報がリポジトリに平文で残っていた問題を発見し、`.gitignore` 管理・伏せ字化で対応。Git履歴に一度含めた機密情報は通常のコミットでは消えず、完全な除去には `git filter-repo` 等による履歴書き換えが必要という制約も理解。

・マルチエージェント（サブエージェント）による並行開発の実践：複数のAIエージェントに作業を分担させる際、担当範囲が重複すると同一ファイルへの二重指示・上書き競合が発生することを実際に経験し、担当ファイル単位でのスコープ分割、競合検知時の作業停止と原因切り分けを行うワークフローを構築。

・周辺機能の実装経験：Contact Form 7 + ハニーポット方式によるスパム対策、GLightbox（CDN経由・jQuery非依存）を用いた画像ギャラリーのライトボックス実装、画像ライセンス（CC BY-NC-ND等の制限付きライセンス）の確認とライセンスフリー素材への差し替え対応。

・ドキュメント整備：第三者視点でのプロジェクトセルフレビュー（`PROJECT_REVIEW.md`）を実施し、指摘事項に優先度をつけて段階的に解消。案件固有ルール（`AGENTS.md`）と汎用テンプレート（`AGENTS_TEMPLATE.md`）を分離し、次案件へ再利用可能な形式で運用ルールを文書化。
